export const easyText: string =
	"Lorem, ipsum dolor sit amet consectetur adipisicing elit. Quibusdamdelectus sit nobis ad autem deserunt animi alias magni, eos ducimus explicabo, consequuntur a quam ratione dignissimos, cumque at quasi nisi dolore! Pariatur accusamus officia doloribus reprehenderit eum nemo. Odio, vitae! Error hic modi maxime possimus exercitationem commodi magni eum asperiores";

/* if (typedTxt[i] === spltdText[i][0]) {
				setSplitedText(
					spltdText.map((txt, index) =>
						index === i
							? (txt = [txt[0], 1])
							: index > i
							? (txt = [txt[0], 0])
							: txt
					)
				);
			} else {
				setSplitedText(
					spltdText.map((txt, index) =>
						index === i
							? (txt = [txt[0], -1])
							: index > 1
							? (txt = [txt[0], 0])
							: txt
					)
				);
			}
		} else {
			setSplitedText(spltdText.map((txt, index) => (txt = [txt[0], 0])));
		} */
